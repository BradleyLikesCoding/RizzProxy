const config = {
    requireLogin:false, /* This will make the website go to a 404 page to make 
    it harder to be found and blocked. To get past the 404, press the keys WRIZ on your keyboard 
    at the same time. Then right after you release it, quickly hold down the keys PRXY at the 
    same time until it asks for a passcode. */
    password:"rizzproxy=ontop" // default password is "rizzproxy=ontop" but you can change it here
};
  
export default config;